import enHomeText from "./home";
import enFooterText from "./footer";
import enNavbarText from "./navbar";

const enText = {
  home: enHomeText,
  footer: enFooterText,
  navbar: enNavbarText,
};

export default enText;
